# dependencies
node_modules/

# build output
dist/
dist-ssr/

# local env / secrets
.env
.env.local
.env.*.local

# logs
npm-debug.log*
*.log

# large local study material (copyrighted PDFs etc.) — keep out of git
resources/

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
Thumbs.db

# keep legacy reference but ignore its noise — tracked intentionally
